# Experiments

`pi05_flow_parity/` is a real one-batch integration experiment with fixed noise
and Euler grid. `motivation/` documents the multi-suite paired-seed motivation
and main protocols, whose comparison inputs live under `configs/experiments/`.
Mathematical synthetic fixtures live only in `tests/`; no synthetic generator is
presented as an experiment.
