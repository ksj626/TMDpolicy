# Processors

`protocols.py` types official batch preprocessors/postprocessors. Implementations come from pinned LeRobot checkpoints so normalization, image keys, state layout, and canonical action conversion are not reimplemented silently.

