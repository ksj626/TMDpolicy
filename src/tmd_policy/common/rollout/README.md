# Rollout

`alignment.py` enforces `L` executed actions, `L+1` resulting states, and an all-real boolean prefix. The legacy collector now accumulates success and separately records environment truncation and local limits. Rollout execution is an explicit expensive script only.

