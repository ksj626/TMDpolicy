# Tasks

`identity.py` defines collision-resistant canonical task UIDs from benchmark/suite/index/instruction/BDDL identity and hashes. `registry.py` maps many dataset episodes to one canonical task while rejecting ambiguous joins. `inspection.py` prints cached dataset episode/instruction mappings and unresolved entries before work.

